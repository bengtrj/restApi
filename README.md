FIRST FRIDAY TALK - **RESTful API**

This repository has all the code needed to run and test a simple User RESTful API.

**To test:**

`$./gradlew test`


**To run the application:**

`$./gradlew bootRun`
